export const WayanadExample = () => (
  <Note>
    Examples use the video draft **Wayanad — A Quiet Journey**. Adapt names, media, timing, and layer values to fit your own draft.
  </Note>
)

export const AutosaveNote = () => (
  <Note>
    Editor changes are autosaved. After a substantial edit, allow a moment for synchronization before closing the tab or changing networks.
  </Note>
)

export const FramePrecision = () => (
  <Tip>
    The editor evaluates timing in whole frames. Zoom into the timeline before a precise trim or keyframe move, then verify the result one frame at a time.
  </Tip>
)

export const SelectionWarning = () => (
  <Warning>
    Inspector edits apply to the current selection. Check the canvas outline and highlighted timeline rows before changing a shared property.
  </Warning>
)

export const TechnicalTruth = () => (
  <Note>
    Technical detail: visual state is evaluated for the current frame. Base layer properties, layout constraints, and keyframe tracks are resolved before the canvas is painted.
  </Note>
)
